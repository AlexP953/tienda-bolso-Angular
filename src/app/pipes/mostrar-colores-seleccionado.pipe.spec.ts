import { MostrarColoresSeleccionadoPipe } from './mostrar-colores-seleccionado.pipe';

describe('MostrarColoresSeleccionadoPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarColoresSeleccionadoPipe();
    expect(pipe).toBeTruthy();
  });
});
